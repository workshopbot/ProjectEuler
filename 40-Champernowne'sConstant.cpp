#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    unsigned int LENGTH = 150;
    float j = 1, result = 0;

    char Numbers[1000001];

    for (int i = 0; i < LENGTH; i++)
    {
        Numbers[i] = i;
        result = powf(10, j);
        if (result == 10)
        {

            continue;
        }
        else if (result =)

            cout << Numbers[i] << "\n";
    }
}
