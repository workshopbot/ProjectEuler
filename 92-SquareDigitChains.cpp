#include <iostream>
#include <math.h>
#include <vector>

using namespace std;

uint DIGITS[8] = {0};
void Digits(&DIGITS, uint);

int main()
{
    uint Numbers = 1;
    // uint LIMIT = 10000000;
}

void Digits(&DIGITS, uint _num)
{
    uint temp = 0, itt = _num;
    while (itt > 0)
    {
    }
}
